#include <stdio.h>
#define PI 3.14
int main()
{float r,parameter;
printf("enter the radius");
scanf("%f",&r);
parameter=2*PI*r;
printf("the parameter of the raduis %f is %f",r,parameter);
return 0;
}